import { Config } from './types';

export const config: Config = {
  intervalMinutes: 5,
  collectionName: 'string',
  docName: 'string',
  searchParams: [
    {
      query: 'string',
      locale: 'ja'
    }
  ]
};
